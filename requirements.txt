# ============================================================
# Neuroscience-baseline  —  Python dependencies
# Tested with Python 3.9 / 3.10 / 3.11
# ============================================================

# --- Core ---------------------------------------------------
numpy>=1.21.0,<2.0.0
scipy>=1.7.0
scikit-learn>=1.0.0
scikit-learn-extra>=0.2.0
matplotlib>=3.4.0
seaborn>=0.11.0
pyyaml>=5.4.0

# --- Deep learning ------------------------------------------
# PyTorch 2.x is recommended.  Install the build that matches
# your CUDA version from https://pytorch.org/get-started/locally/
# Example (CUDA 11.8):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Example (CPU only):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=1.13.0
torchvision>=0.14.0

# --- BindsNET (required for Diehl & Cook baseline) ----------
# Install from GitHub to get PyTorch 2.x compatibility fixes:
#   pip install git+https://github.com/BindsNET/bindsnet.git
# The PyPI release (0.2.7) has a torch._six import error on
# PyTorch >= 2.0.  If you only need the non-SNN baselines you
# can skip this dependency entirely.
git+https://github.com/BindsNET/bindsnet.git

# --- Optional: fast similarity search -----------------------
# faiss-cpu>=1.7.0        # CPU-only FAISS
# faiss-gpu>=1.7.0        # GPU FAISS (needs matching CUDA)
